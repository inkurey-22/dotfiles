##
## EPITECH PROJECT, 2025
## day02
## File description:
## Makefile
##

# GHC compiler
GHC = ghc

# Source files (all .hs files in the directory)
SOURCES = DoOp.hs

# Object files (replace .hs with .o)
OBJECTS = $(SOURCES:.hs=.o)

# Output binary
BINARY = doop

.PHONY: all clean

all: $(BINARY)

# Link object files to create the binary
$(BINARY): $(OBJECTS)
    $(GHC) -o $(BINARY) $(OBJECTS)

# Compile .hs files to .o files
%.o: %.hs
    $(GHC) -c $<

clean:
    rm -f $(BINARY) *.hi *.o