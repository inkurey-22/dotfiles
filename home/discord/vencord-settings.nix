{
  useQuickCss = true;

  # Add or remove plugin entries here.
  # The key must match the Vencord plugin name.
  plugins = {
    MessageLogger = {
      enabled = true;
    };

    FakeNitro = {
      enabled = true;
    };
  };
}
